<table>
    <tr>
        <th>Name</th>
        <th>Color</th>
        <th>Size</th>
        <th>SKU</th>
        <th>Type</th>
    </tr>
        <td><%= @product.name %></td>
        <td><%= @product.color %></td>
        <td><%= @product.size %></td>
        <td><%= @product.sku %></td>
        <td><%= @product.category %></td>
    </tr>
</table>
<hr>
<%= button_to "More Info...", product_product_material_path(@product), method: "get", class: "btn btn-primary" %><br>
<%= button_to "Edit", edit_product_path(@product), method: :get, class: "btn btn-primary" %><br>
<%= button_to "Delete", product_path(@product), method: :delete, class: "btn btn-primary",data: {confirm: "Are you sure?"} %><br>
<%= button_to "Add Material to Product", new_product_product_material_path(@product), method: "get", class: "btn btn-primary" %><hr>
<%= button_to "Back", :back, class: "btn btn-primary", method: :get %>