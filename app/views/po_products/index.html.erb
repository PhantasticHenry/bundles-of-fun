<h2 class="po-index-heading mb-4">Products for </h2>

<table>
    <tr>
        <th>Name</th>
        <th>Color</th>
        <th>Size</th>
        <th>SKU</th>
        <th>Category</th>
        <th>Purchase Order</th>
        <% @po_products.each do |p| %>
    </tr>
        <td><%= button_to p.product.name, product_path(p), method: "get" %></td>
        <td><%= p.product.color %></td>
        <td><%= p.product.size %></td>
        <td><%= p.product.sku %></td>
        <td><%= p.product.category %></td>
        <td><%= p.purchase_order.po %></td>
    </tr>
        <% end %>
</table>
<br>


